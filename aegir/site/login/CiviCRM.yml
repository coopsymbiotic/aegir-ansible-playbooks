---

# FIXME hardcoded admin login
- name: Generate a one-time-link
  become: true
  become_user: aegir
  shell:
    chdir: "{{ site_path }}"
    cmd: "cv url civicrm/dashboard --login --user=admin"
  register: onetime
