#!/bin/bash

# {{ ansible_managed }}
#
# coopsymbiotic/ops#163
# Allow up to {{ aegir_getlock_maxcrons }} crons to run at the same time.
# This can be adjusted using the aegir_getlock_maxcrons Ansible variable.

{% for n in range(1, aegir_getlock_maxcrons|int + 1) -%}
flock --verbose -w {{ aegir_getlock_timeout }} /tmp/aegir-getlock-$UID-{{ n }}.lock "$@"{{ ' || ' if not loop.last }}
{%- endfor %}
